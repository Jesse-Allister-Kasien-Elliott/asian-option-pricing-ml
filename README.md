# Asian Option Pricing: MC vs ML

Compares Monte Carlo pricing for arithmetic Asian options with a neural network trained on Monte Carlo data. Monte Carlo becomes more accurate as the number of paths increases but also becomes slower. The neural network gives immediate price estimates with a small error.

Result: much faster pricing with a small loss in accuracy.
